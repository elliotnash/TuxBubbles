using Gtk 4.0;
using Adw 1;

template $TuxBubblesConversationListPage: Adw.Bin {
  child: Adw.Clamp {
    child: Gtk.ListView list_view {
      // Placeholder factory/model; real model wired in Vala
    };
  };
}
