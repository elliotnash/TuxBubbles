using Gtk 4.0;
using Adw 1;

template $TuxBubblesConversationViewPage: Adw.Bin {
  child: Adw.ToolbarView toolbar_view {
    content: Gtk.ScrolledWindow scroller {
      child: Gtk.ListView messages_view {};
    };
  };
}
